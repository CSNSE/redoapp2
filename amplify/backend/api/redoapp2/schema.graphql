type Equation @model @auth(rules: [{allow: public}]) {
  id: ID!
  equation: String
  intercepts: String
  domain: String
  range: String
  image: String
}

type Note @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  image: String
  author: String
}
 